mixin wm_logo
  .wine-merchant-logo
      .row
          .circle__solid__red
          .circle__solid__red
          .circle__solid__red
      .row
          .circle__solid__red
          .circle__solid__red
      .row
          .circle__solid__red
      p.wmlogo-wine WINE
      p.wmlogo-merchant MERCHANT

mixin navlist(nav_list)
    li
        a(href=nav_list.link)= nav_list.name
        if nav_list.sub.length > 0
            ul
                each nav_list_lv2 in nav_list.sub
                    +navlist(nav_list_lv2)


mixin wines(wine)
    .wine__item 
        img(src='images/#{wine.name}.png',title="#{wine.name}")
        .wine__title 
            strong #{wine.brand} 
            | #{wine.title}
        .wine__price
            span $#{wine.price} bottle  
            span $#{wine.caseprice} case
        .wine__rate
            - var i= 0
            while i < wine.rate
                span.star.active
                - i++
            while i >= wine.rate && i < 5
                span.star
                - i++
            span.wine__review #{wine.noOfReviews} reviews
        a(href="#").view__btn VIEW WINE
        a(href="#").addToCart__btn ADD TO CART


mixin recipesItem(imgurl,imgtitle,title,desc,linktitle, link)
    .recipes__item
        .recipe__img
            img(src= imgurl, alt= imgtitle, title= imgtitle)
            .img__title= imgtitle
        h5= title
        .recipe__desc= desc
        a(href="#{link}").recipe__link= linktitle

    